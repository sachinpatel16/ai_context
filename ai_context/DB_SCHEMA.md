# Database Schema

Define the database tables, columns, types, and relationships here.

Example:

- `users` (id PK, email, name, created_at)
- `items` (id PK, owner_id FK->users.id, title, created_at)

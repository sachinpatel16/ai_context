# API Documentation

This document lists public and internal API endpoints, request/response examples, and authentication details.

Example endpoint template:

- `GET /health` — returns system health
- `POST /v1/items` — create an item (auth required)
